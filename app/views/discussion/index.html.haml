%h1.page_title All discussions

%a.btn.btn-primary.mb20#add-tip{ href: "discussion/new" }
  Add A Discussion

- if @discussions.any?
  = table_for(@discussions) do |t|
    = t.data do
      - t.cell(:title) { |e| link_to e.title, discussion_path(e), class: "tip-title" }
- else
  No discussions found.

