%h1.page_title All tips

- if @tips.any?
  = table_for(@tips) do |t|
    = t.data do
      - t.cell(:title) { |e| link_to e.title, "tips/#{e.id}", class: "tip-title" }
      - t.cell(:author, cell_html: {class: "author"})
- else
  No efforts found.
